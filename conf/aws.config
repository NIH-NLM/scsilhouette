wave.enabled = true                     // Enable Wave containers for efficient execution
wave.aws.region = 'us-east-2'            // Ensure containers are optimized for AWS

// Enable monitoring and logging
timeline.enabled = true
trace.enabled = true
dag.enabled = true

